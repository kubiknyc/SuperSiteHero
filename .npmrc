# Explicitly set script shell to cmd.exe on Windows
# This prevents issues where npm might use a different shell (e.g., /bin/sh)
# that doesn't have node_modules/.bin in its PATH
script-shell=C:\\Windows\\System32\\cmd.exe
