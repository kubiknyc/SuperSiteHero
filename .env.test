# E2E Test Environment Configuration
# This file is used for automated end-to-end testing with Playwright

# Supabase Test Instance
# IMPORTANT: Use a separate Supabase project for testing to avoid affecting production data
VITE_SUPABASE_URL=https://your-test-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-test-anon-key

# Test User Credentials
# These credentials should correspond to a real user in your test Supabase instance
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=Test123!@#
TEST_USER_ID=

# Admin User Credentials (for tests requiring admin privileges)
TEST_ADMIN_EMAIL=admin@example.com
TEST_ADMIN_PASSWORD=Admin123!@#
TEST_ADMIN_ID=

# Test Project Data
# Pre-created test project for E2E tests
TEST_PROJECT_ID=
TEST_PROJECT_NAME=E2E Test Project

# Environment
VITE_APP_ENV=test
VITE_APP_URL=http://localhost:5173

# Email (use console provider for tests to avoid sending real emails)
VITE_EMAIL_PROVIDER=console

# Sentry (disabled for tests)
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=test
VITE_SENTRY_DEBUG=false

# Feature Flags (can be used to enable/disable features during testing)
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_ERROR_REPORTING=false

# Test Database Reset
# Set to 'true' to reset test database before running tests (WARNING: destructive)
RESET_TEST_DB=false

# Playwright Options
PWDEBUG=0
CI=false
